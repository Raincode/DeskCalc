#pragma once

enum class TType : char {
	Plus = '+',
	Minus = '-',
	Mul = '*',
	Div = '/',
	Mod = '%',

	Fac = '!',

	LParen = '(',
	RParen = ')',
	LBrace = '{',
	RBrace = '}',
	LBracket = '[',
	RBracket = ']'
};

struct Token {

};